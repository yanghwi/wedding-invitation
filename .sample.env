# 이 파일은 .env 파일의 샘플입니다
# 실제 사용 시에는 이 파일을 복사하여 .env.local로 저장하고 실제 값을 입력하세요

# 네이버 지도 클라이언트 ID (https://console.ncloud.com/ 에서 발급 가능)
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=your_naver_map_client_id_here

# Slack 웹훅 URL (참석 여부 알림용)
# Slack 워크스페이스 > 채널 설정 > 앱 추가 > Incoming Webhooks에서 생성
NEXT_PUBLIC_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXXXX/XXXXX/XXXXXXXXXXXXXXX

# 사이트 URL (배포 후 설정)
NEXT_PUBLIC_SITE_URL=https://your-wedding-invitation-site.com

# 개발 모드 설정 (개발 중에는 true, 배포 시에는 false로 설정)
NEXT_PUBLIC_DEV_MODE=true
